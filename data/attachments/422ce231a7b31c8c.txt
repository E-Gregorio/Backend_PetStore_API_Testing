// Verificar que el código de estado sea 200 OK
pm.test("Código de estado es 200 OK", function () {
    pm.response.to.have.status(200);
});

// Verificar que la respuesta sea un JSON válido
pm.test("La respuesta es un JSON válido", function () {
    pm.response.to.be.json;
});

// Verificar que la respuesta contiene la estructura esperada (con 'code', 'type', y 'message')
pm.test("La respuesta contiene los valores esperados", function () {
    var jsonResponse = pm.response.json();
    pm.expect(jsonResponse).to.have.property("code");
    pm.expect(jsonResponse).to.have.property("type");
    pm.expect(jsonResponse).to.have.property("message");
});
