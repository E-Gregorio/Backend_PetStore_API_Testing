name: Postman API Tests
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
     
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
         
      - name: Install Newman and Reporters
        run: |
          sudo npm install -g newman@latest
          sudo npm install -g newman-reporter-htmlextra
          sudo npm install -g newman-reporter-allure
          newman --version
     
      - name: Clean and Create Directories
        run: |
          sudo rm -rf newman allure-results allure-report
          sleep 2
          sudo mkdir -p newman
          sudo mkdir -p allure-results
          sudo chmod -R 777 newman allure-results
     
      - name: Set up Allure
        run: |
          sudo apt-get update
          sudo apt-get install -y default-jre
          curl -o allure-2.24.1.zip -OL https://repo.maven.apache.org/maven2/io/qameta/allure/allure-commandline/2.24.1/allure-commandline-2.24.1.zip
          sudo unzip allure-2.24.1.zip -d /opt/
          sudo ln -sf /opt/allure-commandline-2.24.1/bin/allure /usr/bin/allure
          which allure
          allure --version
     
      - name: Run Newman Tests
        continue-on-error: true
        run: |
          newman run postman/collection.json \
          -e postman/environment.json \
          -r cli,htmlextra,allure \
          --reporter-htmlextra-export="newman/report.html" \
          --reporter-allure-export="allure-results" \
          --suppress-exit-code
     
      - name: Generate Allure Report
        if: always()
        run: |
          allure generate allure-results --clean -o allure-report
     
      - name: Upload Newman Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: newman-report
          path: newman/
          if-no-files-found: warn
     
      - name: Deploy Report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-report

      - name: Upload Allure Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: allure-results/
          if-no-files-found: warn